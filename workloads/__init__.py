"""NovaSteel pillar workloads (P1–P4) — decision-support models.

Regular package so the pillar workloads can be built into the ``novasteel-workloads`` wheel
and ``%pip install``-ed into a Fabric Spark session (see platform/scripts/deploy_workload_wheels.py).
"""
